name: RecipeWizard
options:
  bundleIdPrefix: com.recipewizard
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "15.0"
  createIntermediateGroups: true

packages:
  GoogleSignIn:
    url: https://github.com/google/GoogleSignIn-iOS
    from: "7.0.0"

settings:
  base:
    SWIFT_VERSION: "5.9"
    MARKETING_VERSION: "1.0.0"
    CURRENT_PROJECT_VERSION: "1"
    DEVELOPMENT_TEAM: ""   # Set your Team ID here after opening in Xcode

targets:
  RecipeWizard:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: RecipeWizard
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.recipewizard.app
        INFOPLIST_FILE: RecipeWizard/Resources/Info.plist
        CODE_SIGN_ENTITLEMENTS: RecipeWizard/Resources/RecipeWizard.entitlements
    entitlements:
      path: RecipeWizard/Resources/RecipeWizard.entitlements
    info:
      path: RecipeWizard/Resources/Info.plist
      properties:
        CFBundleDisplayName: RecipeWizard
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        NSAppTransportSecurity:
          NSAllowsLocalNetworking: true
        NSPhotoLibraryUsageDescription: "RecipeWizard uses photos to identify recipes."
        GIDClientID: "313197419268-80pj3e1pq21imlofmpvkutrgaj9or1fj.apps.googleusercontent.com"
        CFBundleURLTypes:
          - CFBundleURLSchemes:
              - "com.googleusercontent.apps.313197419268-80pj3e1pq21imlofmpvkutrgaj9or1fj"
    dependencies:
      - package: GoogleSignIn
        product: GoogleSignIn
      - package: GoogleSignIn
        product: GoogleSignInSwift

  ShareExtension:
    type: app-extension
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: ShareExtension
      - path: Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.recipewizard.app.ShareExtension
        INFOPLIST_FILE: ShareExtension/Info.plist
        CODE_SIGN_ENTITLEMENTS: ShareExtension/ShareExtension.entitlements
    entitlements:
      path: ShareExtension/ShareExtension.entitlements
    info:
      path: ShareExtension/Info.plist
      properties:
        CFBundleDisplayName: RecipeWizard
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        NSExtension:
          NSExtensionPointIdentifier: com.apple.share-services
          NSExtensionPrincipalClass: "$(PRODUCT_MODULE_NAME).ShareViewController"
          NSExtensionAttributes:
            NSExtensionActivationRule:
              NSExtensionActivationSupportsWebURLWithMaxCount: 1
              NSExtensionActivationSupportsText: true
    dependencies:
      - embed: false
        target: RecipeWizard
        link: false
      - package: GoogleSignIn
        product: GoogleSignIn
